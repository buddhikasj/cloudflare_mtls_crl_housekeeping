{
  "name": "cron-container-crl-housekeeping",
  "description": "Scheduled container for CRL housekeeping - fetches, validates, and maintains Certificate Revocation Lists",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.5.2",
    "wrangler": "^4.37.1"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.10"
  }
}
